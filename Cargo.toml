[package]
name = "dxlib-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
dxlib-ffi = {path = "../dxlib-ffi/",optional = true}
[features]
dxlib-ffi = ["dep:dxlib-ffi"]
